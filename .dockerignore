# Keep the Docker build context clean: deps and build output are produced inside the image, and macOS junk must not end up in the served site.

node*modules
dist
.git
.astro
.idea
.DS_Store
.AppleDouble
.LSOverride
.*\*
.Spotlight-V100
.Trashes
