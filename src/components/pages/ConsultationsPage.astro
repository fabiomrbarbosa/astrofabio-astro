---
import { getEntry } from "astro:content";
import { defaultLocale, type Locale, ui } from "../../i18n/site";

import iconNatal from "../../assets/icons/icon-natal.svg?raw";
import iconNatalSolarReturn from "../../assets/icons/icon-natal-solar-return.svg?raw";
import iconHorary from "../../assets/icons/icon-horary.svg?raw";
import iconElective from "../../assets/icons/icon-elective.svg?raw";
import iconSynastry from "../../assets/icons/icon-synastry.svg?raw";
import BookingForm from "../BookingForm.astro";
import HeroBackground from "../HeroBackground.astro";

const icons: Record<string, string> = {
	natal: iconNatal,
	"natal-solar-return": iconNatalSolarReturn,
	horary: iconHorary,
	elective: iconElective,
	synastry: iconSynastry,
};

const locale = (Astro.currentLocale ?? defaultLocale) as Locale;
const entry = await getEntry("consultations", locale);
const content = entry!.data;
const { bookCta, learnMore } = ui[locale];
---

<HeroBackground orb1="var(--color-tertiary)" orb2="var(--color-accent)" flip />

<section
	class="relative flex min-h-[70vh] w-full items-center overflow-hidden pt-[calc(var(--spacing-section)*2)] pb-[calc(var(--spacing-section)*1.25)]"
>
	<div
		class="relative z-2 mx-auto w-full max-w-(--width-content) px-(--spacing-outer) text-center"
	>
		<h1
			class="mb-6 font-serif text-7xl leading-none tracking-tight max-md:text-5xl"
		>
			{content.hero.heading}
		</h1>
		<p class="mx-auto max-w-2xl text-l md:text-xl">{content.hero.lead}</p>
		<div class="mt-8 flex items-center justify-center gap-4 max-md:flex-col">
			<a href="#book" class="button">{bookCta}</a>
			<a href="#cards-track" class="button button-alt">{learnMore}</a>
		</div>
	</div>
</section>

<div
	id="cards-track"
	class="cards-track mx-auto max-w-(--width-wide) px-(--spacing-outer)"
>
	{
		content.types.map((type, i) => (
			<article class="card rounded-2xl" style={{ "--i": i }}>
				<div class="card-header">
					{type.discipline && (
						<p class="text-secondary mb-2 text-xs font-semibold tracking-widest uppercase">
							{type.discipline}
						</p>
					)}
					<h2 class="card-title mb-6 font-serif">{type.title}</h2>

					<div
						class="card-icon w-full"
						aria-hidden="true"
						set:html={icons[type.id]}
					/>
				</div>

				<div class="card-content">
					<div class="card-body">
						{type.paragraphs.map((p) => (
							<p set:html={p} />
						))}
					</div>{" "}
					<a href="#book" data-type={type.id} class="button mt-8 inline-block">
						{bookCta}
					</a>
				</div>
			</article>
		))
	}
</div>

<script>
	document
		.querySelectorAll<HTMLAnchorElement>("a[data-type]")
		.forEach((link) => {
			link.addEventListener("click", () => {
				const typeSelect = document.getElementById(
					"cf-type",
				) as HTMLSelectElement | null;
				if (!typeSelect) return;
				typeSelect.value = link.dataset.type!;
				typeSelect.dispatchEvent(new Event("change"));
			});
		});

	document.addEventListener("DOMContentLoaded", () => {
		const cards = document.querySelectorAll(".card");

		const updateCards = () => {
			if (window.innerWidth <= 641) return;

			const viewportHeight = window.innerHeight;
			const topOffset = viewportHeight * 0.005; // 10vh
			console.log(topOffset);

			cards.forEach((card, index) => {
				// We only effect cards that have a next sibling card
				if (index === cards.length - 1) return;

				const nextCard = cards[index + 1];
				const nextRect = nextCard.getBoundingClientRect();

				// Calculate how far the next card is from the bottom of the viewport up to the top offset
				// When nextRect.top is at viewportHeight, progress is 0.
				// When nextRect.top reaches topOffset, progress is 1.

				const dist = nextRect.top - topOffset;
				const range = viewportHeight - topOffset;

				// Progress goes from 0 (next card just entering view) to 1 (next card hits top)
				// Actually, we want scale to decrease as next card goes UP.
				// So when next card is at bottom, scale is 1.
				// When next card is at top, scale is 0.95.

				let progress = 1 - dist / range;

				// Clamp
				if (progress < 0) progress = 0;
				if (progress > 1) progress = 1;

				// Apply effects
				const scale = 1 - progress * 0.1; // Scale down by 10%

				(card as HTMLElement).style.transform = `scale(${scale})`;
			});

			requestAnimationFrame(updateCards);
		};

		window.addEventListener("scroll", () => {
			requestAnimationFrame(updateCards);
		});

		// Initial run
		updateCards();
	});
</script>

<BookingForm />

<style>
	/* ── Track ───────────────────────────────────────────────────────── */

	.cards-track {
		padding-bottom: 2rem;
	}

	/* ── Card ── */

	.card {
		display: grid;
		grid-template-columns: 1fr 1.75fr;
		gap: 3rem;
		align-items: start;
		background-color: var(--color-base);
		border: 1px solid var(--color-overlay);
		padding: 3rem;
		margin-bottom: 2rem;
		box-shadow: 0 1px 8px color-mix(in srgb, var(--color-ink) 4%, transparent);
	}

	@media (min-width: 641px) {
		.card {
			position: sticky;
			top: calc(7rem);
			margin-bottom: 7.5vh;
			z-index: calc(var(--i) + 1);
		}
	}

	@media (max-width: 640px) {
		.card {
			grid-template-columns: 1fr;
			gap: 1.5rem;
			padding: 2rem 1.5rem;
			border-radius: 12px;
		}
	}

	/* ── Icon ────────────────────────────────────────────────────────── */

	.card-icon :global(svg) {
		display: block;
		width: 100%;
		max-width: 10rem;
		height: auto;
	}

	/* ── Card title ─────────────────────────────────────────────────── */

	.card-title {
		font-size: clamp(1.75rem, 4vw, 2.5rem);
		line-height: 1.15;
		color: var(--color-contrast);
	}

	/* ── Card body (set:html descendants) ───────────────────────────── */

	.card-body p {
		margin-bottom: 1rem;
		line-height: 1.7;
		color: var(--color-ink);
	}

	.card-body p:last-child {
		margin-bottom: 0;
	}

	.card-body strong {
		color: var(--color-contrast);
		font-weight: 600;
	}
</style>
